"use strict" // treat all JS code as a newer version
// alert("hello") // we r using node not browser

/* avoid semi-colons because it doesnot matters in java script */

/*
number => 2 power 53 (max)
bigInt => for stock market, trading
string => " "
boolean => true/false
null => standalone value
undefined => no defination for variable
symbols => unique
objects
*/

let age = 18
let name_full = "Satyam"
let isLoggedIn = false
let state = null
let email

console.table([age, name_full, isLoggedIn, state, email])
console.log(typeof null) // null is an object
